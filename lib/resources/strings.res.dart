// ignore_for_file: constant_identifier_names
String apiBaseUrl = 'https://riilfit-api.onrender.com';

const countryCodeWithPlus = '+234';

const countryCode = '234';

///Error Messages
const passwordTooWeak = 'password too weak';
const passwordResetSuccessfulMessage =
    'Password reset successfully, Kindly sign in to continue';
const errorMessage =
    "Sorry about this, Something's wrong but we are fixing it.";
const networkError =
    'No internet connection, Kindly check your internet to have a seamless experience.';

///
const maxJsonStringLength = 8192;

const passwordRegEx =
    r'^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$#!%*?&])[A-Za-z\d@$#!%*?&]{8,}$';

const pinLength = 6;
