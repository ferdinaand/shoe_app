const superFastDuration = Duration(milliseconds: 50);
const fastDuration = Duration(milliseconds: 200);
const midDuration = Duration(milliseconds: 400);
const slowDuration = Duration(milliseconds: 800);
